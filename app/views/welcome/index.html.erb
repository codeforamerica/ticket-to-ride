<div class="welcome">
  <h2>Welcome to Ticket to RIDE!</h2>
  <p>Ticket to RIDE is a new tool that lets you register your child for K-12 public schools in Rhode Island.</p>
  <p>Begin by choosing your child's school district.</p>
  <div class="enrollment-form">
    <%= form_tag(request.original_url, method: :post) %>
      <%= select_tag 'district_url', options_from_collection_for_select(District.all, 'url', 'name') %>
      <%= submit_tag 'Go', class: 'btn btn-success pull-right'%>
  </div>

  <div class="enrollment-form">
    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
      <%= form_tag(request.original_url, method: :post) %>
        <%= select_tag 'district_url', options_from_collection_for_select(District.all, 'url', 'name') %> <span class="caret"></span>
      </button>
  </div>
</div>

<%= render 'shared/scenery_welcome' %>