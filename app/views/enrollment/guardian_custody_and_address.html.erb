<%= render 'enrollment/navigation' %>
<div class="enrollment-form">
  <div>
    My name is
    <span class="edited_text"><%= @guardian.first_name %>
    <%= @guardian.last_name %></span>
    and I am
    <span class="edited_text"><%= @student.first_name %></span>'s
    <!-- TODO: Get values to be grabbed during submit, in case the parent assumes the default and doesn't edit it -->
    <a href="#" id="a_guardian_relationship" data-emptytext="(Aunt)" data-title="What is your relationship to <%= @student.first_name %>?" data-type="select" data-placement="top"></a>.

    I
    <a href="#" id="a_guardian_is_custody_shared" data-emptytext="(do)" data-title="Do you share custody of <%= @student.first_name %>?" data-type="select" data-placement="top"></a>
    share custody of
    <span class="edited_text"><%= @gender_objective_pronoun %></span>
    <span id="shared-custody-period-span">.</span>


    <span id="shared-custody-div" style="display: none"> <!-- TODO: Don't have the display:none inline, move to CSS file -->
      with
      <a href="#" id="a_contact_person_first_name" data-emptytext="(Paul)" data-title="What is the first name of the other guardian?" data-type="text" data-placement="top"></a>
      <a href="#" id="a_contact_person_last_name" data-emptytext="(Murray)" data-title="What is the last name of the other guardian?" data-type="text" data-placement="top"></a>
      (who is <span class="edited_text"><%= @gender_possessive_adjective %></span>
      <a href="#" id="a_contact_person_relationship" data-emptytext="(Uncle)" data-title="What is <%= @student.first_name %>'s relationship to this guardian?" data-type="select" data-placement="top"></a>
      ).
      We
      <a href="#" id="a_guardian_other_guardian_cohabitates" data-emptytext="(do)" data-title="Do you live with the other guardian?" data-type="select" data-placement="top"></a>
      live together.
    </span>

    <span id="shared-pronoun-span">(My/Our)</span>
    address is
    <a href="#" id="a_guardian_mailing_street_address_1" data-emptytext="123 Main St" data-title="What is your address?" data-type="text" data-placement="top" data-value="<%= @student.home_street_address_1 %>"></a>,
    <a href="#" id="a_guardian_mailing_city" data-emptytext="<%= @student.home_city %>" data-title="What is your city?" data-type="text" data-placement="top" data-value="<%= @student.home_city %>"></a>,
    RI,
    <a href="#" id="a_guardian_mailing_zip_code" data-emptytext="02093" data-title="What is your zip code?" data-type="text" data-placement="top" data-value="<%= @student.home_zip_code %>"></a>.
  </div>
</div>

<%= form_for(@guardian, url: wizard_path, method: :put) do |g| %>
    <div id="hidden-form-fields">
      <%= g.text_field :relationship %>
      <input type="text" id="guardian_is_custody_shared"/> <!-- TODO: This should be a checkbox -->
      <%= g.text_field :mailing_street_address_1 %>
      <%= g.text_field :mailing_city %>
      <!-- TODO: Add a mailing state for guardian #1 -->
      <%= g.text_field :mailing_zip_code %>
      <input type="text" id="guardian_other_guardian_cohabitates"/> <!-- TODO: This should be a checkbox -->

      <!-- TODO: If other guardian cohabitates, populate the second guardian's address info and skip the next page -->
      <%= fields_for(ContactPerson.new) do |c| %>
          <%= c.text_field :first_name %>
          <%= c.text_field :last_name %>
          <!--<%= c.text_field :mailing_street_address_1 %>-->
          <!--<%= c.text_field :mailing_city %>-->
          <!--<%= c.text_field :mailing_state %>-->
          <!--<%= c.text_field :mailing_zip_code %>-->
          <%= c.text_field :relationship %>
      <% end %>

    </div>

    <ul class="pager">
      <li><%= link_to "Previous", previous_wizard_path %></li>
      <li><%= g.submit 'Next', class: 'nav-button pull-right' %></li>
    </ul>

<% end %>

<%= render 'shared/scenery_guardian' %>